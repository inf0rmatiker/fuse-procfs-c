# fuse-procfs-c

Implementation of an in-memory filesystem, exposed over FUSE, written in C.

## Build Project

Run CMake from a `build/` directory:

```bash
mkdir build && cd build && cmake ..
```

This should generate a `Makefile`.

Run `make` to build the main executable.

